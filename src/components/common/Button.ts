import styled from 'styled-components';

export const Button = styled.button`
  all: unset;
  border-radius: 5px;
  background-color: var(--accent);
  padding: 0.5rem 1rem;
  color: var(--primary);
`;
