import styled from 'styled-components';

export const SellContainer = styled.section`
  background-color: var(--filter);
  backdrop-filter: blur(3px);
  width: calc(95% - 10vmin);
  max-width: 768px;
  min-height: 100%;
  margin: 10vmin auto 0;
  padding: 5vmin;
`;
